export interface Student {
  id: string; // Matrícula
  name: string; // Nome
}

export interface DocumentConfig {
  location: string;
  department: string;
  title: string;
  subtitle: string;
  bodyText: string;
}
